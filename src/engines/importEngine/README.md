The import engine owns source parsing, header detection, proposed/manual
mapping, field classification, row validation, optional derived calculation,
preview summaries, and template-column generation.

Phase 12 is preview-only: it does not save drafts or allocate document numbers.
Full-record imports remain draft-oriented, and duplicate external document
numbers are surfaced as row issues before any future apply step.
